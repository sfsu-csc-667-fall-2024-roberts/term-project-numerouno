<nav>
    <ul class="menu">
        <li><a href="/" class="menu-btn">Home</a></li>
        <li><a href="/lobby" class="menu-btn">Main Lobby</a></li>
        <li><a href="/games/15" class="menu-btn">Game Page</a></li>
        <% if (locals.user ==undefined) { %>
        <div class="flex flex-row items-center">
            <a href="/auth/register" class="menu-btn">Register</a>
        </div>
        <% } %>
        <% if (locals.user ==undefined) { %>
        <div class="flex flex-row items-center">
            <a href="/auth/login" class="menu-btn">Login</a>
        </div>
        <% } %>
        <% if (locals.user !==undefined) { %>
        <div class="flex flex-row items-center">
            <a href="/auth/logout" class="menu-btn">Logout</a>
        </div>
        <% } %>
    </ul>
</nav>

<!-- Styling for the menu buttons -->
<style>
    /* Basic styling for the menu container */
    .menu {
        list-style-type: none;
        padding: 0;
        margin: 0;
        display: flex;
    }

    /* Styling for each button (menu item) */
    .menu-btn {
        background-color: #4CAF50;
        color: white;
        padding: 10px 20px;
        text-align: center;
        text-decoration: none;
        border-radius: 8px;
        margin: 5px;
        display: inline-block;
    }

    /* Hover effect for buttons */
    .menu-btn:hover {
        background-color: #2563eb;
    }

    .flex {
        display: flex;
        align-items: center;
    }

    .flex img {
        margin-left: 10px;
    }
</style>